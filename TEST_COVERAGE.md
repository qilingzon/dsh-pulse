# dsh-pulse 测试覆盖矩阵

> 目的：任何人（包括未来的我）都能一眼看出「测过什么、怎么测的、没测什么」。
> 更新规则：每轮测试后改这张表；第三档（未测）里的条目转档时，把证据链写进来。
> 当前版本：**v0.7.0** ｜ 最后更新：2026-09-25

## 一览

| 层 | 手段 | 状态 |
|---|---|---|
| L1 纯函数 | `node verify.mjs`（**305 条断言**） | ✅ 全绿 |
| L2 组件渲染 | `node smoke.mjs`（**66 条断言**，真装载 client.js + 假 React） | ✅ 全绿 |
| L3 安装器 | 假 DSH home 上实测 install / 幂等重跑 / remove；`install.ps1` 过 PS 5.1 解析 | ✅ 通过 |
| L4 真实浏览器 | gen4-lab 实机，**三轮不同文体**（含一轮多步 + 工具调用），产品 `decodeTokens` 为权威值 | ✅ 关键路径通过 |
| L5 性能 | `node bench.mjs`（每拍 / 字符重数 / 缓冲 / 体积） | ✅ 数字可复算 |
| L6 文档一致性 | README / HARNESS_PLUGIN / 本文件 的数字 ↔ bench & 实测输出 | ✅ 本轮同步过 |

## L1 · 纯函数（verify.mjs，305 条）

覆盖：

- **两位小数 formatter**：补零 / 上界 / 部分命中不伪装 100 / 不同 `decimalPlaces` 调用者行为不变。
- **滑窗算术**：环形裁剪、窗沿锚点、跨度下限、计数器回退。
- **`formatRate`** 格式规则；**`calibrateRatio`** 夹紧；**`exactOutputTokens`** 优先级。
- **`displayKey`** 渲染闸门指纹（含「估计↔真值切换必须换指纹」）。
- **五类字符分类**（v0.5.0 新增）：`classOf` / `classOfCode` 同口径、CJK 区间、全角空格、空串不崩、
  `countClasses` 五桶互斥且完备（`countsTotal` == 字符串长度）、`countsUnits` 与旧加权口径一致。
- **`liveChars` / `liveCounts` / `liveUnits`**：text + reasoning + **工具参数**（v0.5.0 口径变更）都计入。
- **岭回归标定**（v0.5.0 新增）：零数据恒等于先验；`solveLinear` 2×2 精确解与奇异矩阵返回 null；
  24 步合成数据（先验错）总误差降 ≥15%；**先验正确时 24 步 `Σ|Δ| < 0.02`（不被单步噪声带偏）**；
  单步只做有限修正；解夹紧在 `[RATE_MIN, RATE_MAX]`；零字符 / 零 token 的观测被丢弃。
- **持久化**（v0.5.0 新增）：写入→读回逐字段一致；JSON 损坏 / 版本不符 / 维度不符 / 被投毒（负数、NaN）
  / storage 抛错 —— 五种坏输入全部静默降级到先验；桶名一律带 `CAL_KEY` 命名空间。
- **`stepMeter` 端到端**：步骤开始 → 估算 → 结算 → **两条线等比回填** → 第二步复用新速率。
- **结算假尖峰回归测试**（v0.5.0 新增）：30 秒步骤（比窗口长）结算后，exact 窗斜率必须是真实平均
  32 tok/s 而不是「整步 960 tok ÷ 10s = 96 tok/s」。
- **`readRate` 三态**：`estimate` / `exact` / `idle` 的文案、前缀、tooltip 口径来源。
- **注册面静态核对**：dock 目标、id、order、6 个只读探针、定时器清理、不碰 DOM、不走 RPC、**不发网络请求**。

## L2 · 组件渲染（smoke.mjs，66 条）

真装载 `client.js`（走 `window.__ModuleLoader__.load`），假 React 替身（含跨渲染持久化的 `useRef`），
断言：两枚 pill 文案、`data-pulse-*` 探针初始值、`~` 前缀与 `✓` 后缀、`data-pulse-src` 三态、
tooltip 口径来源与五类标定摊开、null 不占位、定时器挂载/卸载、两拍采样推进（假时钟）。

> 注意：假 React 替身只实现 `createElement/useState/useRef/useEffect` 的最小语义。**没有覆盖**：
> React 调度、卸载竞态、`useChat`/`useProjection` 订阅的真实行为、副作用时序。

## L3 · 安装器

- `install.sh`：`bash -n` 语法过；假 home 实测 install（4 处解析位、16 项 sha256 全 MATCH）、
  幂等重跑、`--remove`（目录清空 + package.json 摘除后与原始逐字一致、备份保留）。
- `install.ps1` / `uninstall.ps1`：**UTF-8 BOM 修复前在 Windows PowerShell 5.1 上解析失败**（中文引号打断），
  修复后 5.1 解析通过；`install.ps1` 对真实 `gen4_home` 执行过（三处解析位 MATCH + profile 注册）。

> **未测**：`--remove` 从未对真实 `gen4_home` 执行过（只在假 home）；文档里的
> 「`cd <profile> && pnpm install` → 重启 → 新开对话」四步收口从未完整走查过
> （lab 服务从三处解析位副本直接加载成功，pnpm install 一步被跳过）。

## L4 · 真实浏览器（最重要的一层）

环境：`gen4_home` / profile `gen4-lab` / headless Chrome 153 + CDP(9222) / 模型 `glm-5.3-flash` /
权威值 = 产品 `sessionStats.decodeTokens`（探针 `data-pulse-exact`）。

### v0.3.1 → v0.4.0：标定比

| 运行 | 种子 | unit | 真实产出 | 真实 tok/unit | 估算误差 |
|---|---|---|---|---|---|
| 1 | 0.5 | 16586 | 10896 | ≈0.80 | −23.9% |
| 2 | 0.62 | 3074 | 2532 | 0.824 | −24.7% |
| 3 | 0.62 | 467 | 373 | 0.799 | −22.4% |
| 4 | 0.80 | 490 | 400 | 0.816 | −2.0% |

### v0.5.0：三轮不同文体（含多步 + 工具）

| 轮次 | 内容 | 步数 | 真值产出 | 估算产出 | 估算误差 | 结算首帧 / 本步真值 |
|---|---|---|---|---|---|---|
| R1 | 中文说明文 | 1 | 614 | 605 | **−1.5%** | 32 / 34.1 = **0.94** |
| R2 | 英文散文 | 1 | 560 | 578 | **+3.2%** | 26 / 29.1 = **0.89** |
| R3 | 中文 + 写文件工具调用 | 3 | 2338 | 2477 | **+5.9%** | 39 / 52.2 = **0.75** |

| 验证点 | 结果 |
|---|---|
| 插件在真实 DSH web 里装载并渲染两枚 pill | ✅ |
| 宿主流式期不报 token 数（`decodeTokens` 恒为基线） | ✅ v0.4.x 四次 + v0.5.0 三轮复现 |
| `data-pulse-src` 三态迁移正确（含三步 turn 逐步翻转） | ✅ 见 README《七》B 的序列 |
| 估计值 / 真值视觉与探针分离 | ✅ 三轮均 `estimate→exact` 正确翻转 |
| **结算后「真值」假尖峰** | ✅ **v0.5.0 修**：修前实测 159 tok/s（真值 37.8），修后比值 0.94 / 0.89 / 0.75 |
| **字母先验 0.34 偏高 42%** | ✅ **v0.5.0 修**：实测 0.2389 → 改为 0.24；英文误差 +39.1% → +3.2% |
| **工具参数未计入导致低估 16.4%** | ✅ **v0.5.0 修**：计入 `name` / `argsRaw` 后 → +5.9% |
| **回归在先验正确时不漂移** | ✅ 三轮结算后 rates 与先验同量级（见 README《七》B 结论五） |

## L5 · 性能（bench.mjs）

| 项 | 当前实测 |
|---|---|
| 每拍（`stepMeter + displayKey`，满窗 41 点） | 0.24–0.25 µs |
| 五类字符重数 | 3.19 ns/字符（9750 字 ≈ 31 µs） |
| 流式持续 CPU（2 拍/秒，按 8000 字正文上界） | 62.6 µs/秒 = 0.0063% 单核 |
| 空闲持续 CPU | 0.50 µs/秒 |
| 缓冲 | 恒 41 点（20 万拍后不变） |
| 常驻 | ≈ 1 KiB |
| 发布体积 | 4 文件 47188 B → gzip 17141 B（16.7 KiB） |

> 边界：纯算术，**不含** React 渲染成本；渲染次数已被 `displayKey` 闸门压到空闲 0 次/秒，但**单帧成本未测**。
> 字符重数是 **O(正文长度)** 的 —— 这是 v0.5.0 相对 v0.4.x 唯一新增的持续开销（`0.00004%` → `0.0063%` 单核）。

## 第三档 · 明确未测（回答「全面吗」时以此为准）

| 未测项 | 影响 |
|---|---|
| **其他模型** | 全部实测都在 `glm-5.3-flash`。换模型时前 1~2 步会重新收敛，但**未在第二个模型上验证** |
| **标点 / 空白 / 数字三类先验** | 汉字 0.80 与字母 0.24 是实测值；其余三类按 tokenizer 行为取值，**无独立实测** |
| **真实浏览器里的帧级视觉**（首帧延迟/抖动/空闲 0 帧计数） | 只有采样序列，没有逐帧目视 |
| **后台标签节流**（约 1 次/分钟） | 通用浏览器行为，未在本机验证 |
| **`pnpm install` 收口步** | lab 从三处解析位副本直接加载成功，该步被跳过 |
| **对真实 gen4_home 的 `--remove`** | 只在假 home 执行过 |
| **设置页分区的真机挂载** | v0.7.0 新增：`settings.section` 在真浏览器里**挂得上、画得出**只在组件层（假 React）验过；真机需人工打开设置对话框目视 |
| **`localStorage` 轨迹的长期增长** | v0.7.0 新增：1200 点环形 + 每 5s 快照，单次快照实测 58.6 KiB；多日运行下的键体积未做长观察 |
| **生产 `C:\Users\qiling\.dsh`** | 按 C10/C11：动之前必须 `desktop_backup.ps1` → BACKUP-OK；v0.6.2 起已按此流程安装过 |
| **非 web 客户端** | `client.platform: web`，其余平台不适用 |
| **React 真实调度 / 卸载竞态** | 假 React 替身覆盖不到 |
| **精确的流中 token 数** | **物理不可得**，不是「未测」：provider 只在 finish chunk 上报 usage，harness 也没有分词器（源码证据见 README《七》第一档）。任何插件都做不到 |

## 结论

可以下结论的部分：**估算误差、结算读数真实性、三态分离、多步/英文/工具调用、宿主流式期行为、性能开销** —— 都有实机或可复算证据。
不能下结论的部分：**其他模型、三类非实测先验、帧级视觉** —— 缺测，上面第三档就是缺口清单。
「全面测试过了吗」→ **仍然没有**，第三档就是缺口。
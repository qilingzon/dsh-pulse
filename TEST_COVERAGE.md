# dsh-pulse 测试覆盖矩阵

> 目的：任何人（包括未来的我）都能一眼看出「测过什么、怎么测的、没测什么」。
> 更新规则：每轮测试后改这张表；第三档（未测）里的条目转档时，把证据链写进来。

## 一览

| 层 | 手段 | 状态 |
|---|---|---|
| L1 纯函数 | `node verify.mjs`（73 条断言） | ✅ 全绿 |
| L2 组件渲染 | `node smoke.mjs`（23 条断言，真装载 client.js + 假 React） | ✅ 全绿 |
| L3 安装器 | 假 DSH home 上实测 install / 幂等重跑 / remove；`install.ps1` 过 PS 5.1 解析 | ✅ 通过 |
| L4 真实浏览器 | gen4-lab 实机，四次流式回答，产品 `decodeTokens` 为权威值 | ✅ 关键路径通过 |
| L5 性能 | `node bench.mjs`（每拍 / 缓冲 / 体积） | ✅ 数字可复算 |
| L6 文档一致性 | README 数字 ↔ bench 输出 | ⚠️ 本轮刚同步过，需随改动再同步 |

## L1 · 纯函数（verify.mjs，73 条）

覆盖：两位小数 formatter（补零/上界/不伪装 100）；滑窗算术（环形裁剪、窗沿锚点、跨度下限、计数器回退）；
`formatRate` 格式规则；标定比夹紧；`liveChars` / `liveUnits`（CJK=1 / 非CJK=0.42）加权口径；`exactOutputTokens`
优先级；`displayKey` 渲染闸门指纹；`stepMeter` 端到端（步骤开始/估算/结算/等比回填/第二步复用新比）；
注册面静态核对（dock 目标、id、4 个只读探针、定时器清理、不碰 DOM、不走 RPC）。

## L2 · 组件渲染（smoke.mjs，23 条）

真装载 `client.js`（走 `window.__ModuleLoader__.load`），假 React 替身（含跨渲染持久化的 `useRef`），
断言：两枚 pill 文案、`data-pulse-*` 探针初始值、`~` 前缀、tooltip 口径来源、null 不占位、定时器挂载/卸载、
两拍采样推进（假时钟）。

> 注意：假 React 替身只实现 `createElement/useState/useRef/useEffect` 的最小语义。**没有覆盖**：
> React 调度、卸载竞态、`useChat`/`useProjection` 订阅的真实行为、副作用时序。

## L3 · 安装器

- `install.sh`：`bash -n` 语法过；假 home 实测 install（4 处解析位、16 项 sha256 全 MATCH）、
  幂等重跑、`--remove`（目录清空 + package.json 摘除后与原始逐字一致、备份保留）。
- `install.ps1` / `uninstall.ps1`：**UTF-8 BOM 修复前在 Windows PowerShell 5.1 上解析失败**（中文引号打断），
  修复后 5.1 解析通过；`install.ps1` 对真实 `gen4_home` 执行过（三处解析位 MATCH + profile 注册）。

> **未测**：`--remove` 从未对真实 `gen4_home` 执行过（只在假 home）；文档里的
> 「`cd <profile> && pnpm install` → 重启 → 新开对话」四步收口从未完整走查过
> （lab 服务从三处解析位副本直接加载成功，pnpm install 一步被跳过）。

## L4 · 真实浏览器（最重要的一层）

环境：`gen4_home` / profile `gen4-lab` / headless Chrome 153 + CDP / 权威值 = 产品 `sessionStats.decodeTokens`。

| 验证点 | 结果 |
|---|---|
| 插件在真实 DSH web 里装载并渲染两枚 pill | ✅ |
| 缓存命中 pill 显示真实值 | ✅（实测中见到 `缓存命中 6.13%` 等） |
| 宿主流式期不报 token 数（`decodeTokens` 恒为基线） | ✅ 四次复现 |
| 估算线相对真实产出的误差 | ✅ 见下 |
| 结算瞬间假尖峰 | ✅ 已修（1115 → 最大 40 tok/s，`>60` 采样 0 条） |
| 种子比 0.80 的实测依据 | ✅ 四次独立回答，真实比 0.801/0.824/0.799/0.816 |

```text
| 运行 | 种子 | unit | 真实产出 | 真实 tok/unit | 估算误差 |
| 1    | 0.5  | 16586 | 10896 | ≈0.80 | −23.9% |
| 2    | 0.62 | 3074  | 2532  | 0.824 | −24.7% |
| 3    | 0.62 | 467   | 373   | 0.799 | −22.4% |
| 4    | 0.80 | 490   | 400   | 0.816 | −2.0%  |
```

## L5 · 性能（bench.mjs）

| 项 | 当前实测 |
|---|---|
| 每拍（stepMeter + displayKey，满窗 41 点） | 0.22 µs |
| 持续 CPU（2 拍/秒折算） | 0.44 µs/秒 = 0.000044% 单核 |
| 缓冲 | 恒 41 点（20 万拍后不变） |
| 发布体积 | 4 文件 28774 B → gzip 10497 B（10.3 KiB） |

> 边界：纯算术，**不含** React 渲染成本；渲染次数已被 displayKey 闸门压到空闲 0 次/秒，但**单帧成本未测**。

## 第三档 · 明确未测（回答「全面吗」时以此为准）

| 未测项 | 影响 |
|---|---|
| **多步 turn（含工具调用）** | 每一步会重新标定 + 回填，但步间切换的实际观感未观察 |
| **非中文输出**（英文/代码/混合） | 种子 0.80 只在中文技术文体验证过；英文场景理论差 ±5%（0.34 tok/char 折算），未实机复核 |
| **换模型** | 四次实测同一模型（glm-5.3-flash，lab 默认）；其他模型未测 |
| **真实浏览器里的帧级视觉**（首帧延迟/抖动/空闲 0 帧计数） | 只有采样序列，没有逐帧目视 |
| **后台标签节流**（约 1 次/分钟） | 通用浏览器行为，未在本机验证 |
| **`pnpm install` 收口步** | lab 从三处解析位副本直接加载成功，该步被跳过 |
| **对真实 gen4_home 的 `--remove`** | 只在假 home 执行过 |
| **生产 `C:\Users\qiling\.dsh`** | 按 C10/C11 全程未触碰 |
| **非 web 客户端** | `client.platform: web`，其余平台不适用 |
| **React 真实调度 / 卸载竞态** | 假 React 替身覆盖不到 |

## 结论

可以下结论的部分：**估算线的误差、结算尖峰、宿主流式期行为、性能开销** —— 都有实机或可复算证据。
不能下结论的部分：**多步 turn、非中文内容、其他模型** —— 缺测，README《七》第三档已如实列出。
「全面测试过了吗」→ **没有**，上面第三档就是缺口清单。